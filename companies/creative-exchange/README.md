# Thrive Creative Exchange

Status: **logo lockup added (1-color/2-color/full-color, light/dark bg); favicon needs
re-exporting with the new monogram**. See `../../ASSET-INVENTORY.md` for the full picture and
`../../site/index.html` for the visual brand book.

- `logo/svg/` — 1-color, 2-color, full-color, light/dark bg
- `logo/png/` — 1-color, 2-color, full-color, light/dark bg
- `favicon/tce-favicon-lightbg.svg` / `-darkbg.svg` — still uses the old **TCE** monogram; the
  monogram is changing to **TCX** going forward (not "TC" — that's reserved for Capital MX, see
  `../capital-mx/`), so these need re-exporting
