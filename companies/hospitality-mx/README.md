# Thrive Hospitality MX

Status: **4 unresolved favicon drafts, no logo lockup, no final pick made**. See
`../../ASSET-INVENTORY.md` for the full picture and `../../site/index.html` for the visual brand book.

- `favicon/thrive-hospitality-mx-favicon-variant-{1..4}.svg` — from the old files "Th 1–4",
  near-identical monogram variants. Pick one as final and move the rest to a `99-Archive/` folder.
- `logo/` — empty; needs a full lockup exported from `../../00-shared/logo.afdesign`
